services:
  vpn:
    ports:
      - "${WEBTORRENT_TORRENT_PORT}:${WEBTORRENT_TORRENT_PORT}"
      - "${WEBTORRENT_TORRENT_PORT}:${WEBTORRENT_TORRENT_PORT}/udp"
      - "${PROWLARR_PORT}:${PROWLARR_PORT}"
      - "${SUBTITLE_PROXY_PORT}:${SUBTITLE_PROXY_PORT}"
