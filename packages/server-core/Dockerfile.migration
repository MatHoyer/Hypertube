FROM node:22-alpine

WORKDIR /migration

COPY packages/server-core/prisma ./prisma
COPY packages/server-core/prisma.config.ts ./prisma.config.ts

RUN npm install prisma@7.0.0 dotenv

CMD ["npx", "prisma", "migrate", "deploy"]