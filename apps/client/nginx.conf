events {
  worker_connections  1024;
}

http {
  include   mime.types;

  server {
    listen 80;
    server_name _;

    root    /hypertube/apps/client/dist;
    index   index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
  }
}