services:
  app:
    container_name: hypertube-prod
    build:
      context: .
      dockerfile: ./Dockerfile.prod
    env_file: .env
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public
      - NODE_ENV=PROD
      - PUPPETEER_SKIP_DOWNLOAD=true
      - PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium
      - REDIS_HOST=redis
    ports:
      - ${SERVER_PORT}
    volumes:
      - movies_data:/hypertube/apps/server/downloads
    command: bash -c "pnpm lint && pnpm --filter server prisma:migrate:reset && pnpm --filter server prisma:migrate && pnpm --filter server prisma:seed && pnpm build:app && pnpm start:app"
    networks:
      - hypertube-network
    healthcheck:
      test: ["CMD", "curl", "-f", "${SERVER_URL}/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  downloader:
    container_name: hypertube-downloader-prod
    build:
      context: .
      dockerfile: ./apps/downloader/Dockerfile.prod
    env_file: .env
    environment:
      - REDIS_HOST=redis
      - NODE_ENV=PROD
      - TRANSMISSION_HOST=transmission
      - SERVER_URL=http://app:3000
    command: bash -c "pnpm lint && pnpm build:downloader && pnpm start:downloader"
    volumes:
      - movies_data:/hypertube/apps/downloader/downloads
    networks:
      - hypertube-network
    depends_on:
      - app

networks:
  hypertube-network:
    external: true

volumes:
  movies_data:
